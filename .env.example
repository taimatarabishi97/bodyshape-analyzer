# Colors Code Body Shape Analysis - Environment Variables
# Copy this file to .env and fill in your values

# ============================================================================
# OPERATING MODE
# ============================================================================
# BASIC = Works without backend (no email, no webhooks)
# FULL = Requires backend setup (email + webhooks enabled)
VITE_MODE=BASIC

# ============================================================================
# SUPABASE (Required for both modes)
# ============================================================================
# Get these from: Supabase Dashboard → Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# ============================================================================
# RESEND (Optional - Only needed for FULL_MODE)
# ============================================================================
# Get this from: Resend Dashboard → API Keys
# Used for sending analysis results via email
VITE_RESEND_API_KEY=re_your_api_key_here

# ============================================================================
# APPLICATION URL (Optional - Only needed for FULL_MODE)
# ============================================================================
# Your deployed application URL
# Used for generating access links in emails
VITE_APP_URL=https://your-app.vercel.app

# ============================================================================
# NOTES
# ============================================================================
# - All variables starting with VITE_ are exposed to the browser
# - VITE_MODE controls whether access gate and email features are enabled
# - In BASIC_MODE, users go directly to questionnaire
# - In FULL_MODE, users must provide access token or email first
# - See DEPLOYMENT-CHECKLIST.md for detailed setup instructions